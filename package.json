{
  "name": "space-toursim-website",
  "version": "1.0.0",
  "description": "A multipage website built with Php, Apache, Docker, and Sass.",
  "main": "index.js",
  "scripts": {
    "sass:dev": "sass --watch sass/main.scss public/css/main.css",
    "sass:build": "sass sass/main.scss public/css/main.css --style=compressed",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "sass": "^1.79.4"
  }
}
